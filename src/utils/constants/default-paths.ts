const API = '/api';
const HELLO_WORLD_PATH = `${API}/hello-world`;
const WATSON = `${API}/watson`;
const ORCHESTRATE = `${API}/orchestrate`;

export const paths = {
  helloWord: HELLO_WORLD_PATH,
  watson: WATSON,
  orchestrate: ORCHESTRATE,
};
